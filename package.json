{
  "name": "pmsusermanual",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "validate": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "deploy:manual": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy-manual.ps1",
    "deploy:manual:upload": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy-manual.ps1 -SkipBuild",
    "site:download": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/download-site-index.ps1",
    "site:upload": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/upload-site-index.ps1",
    "deploy:help": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy.ps1 help"
  },
  "dependencies": {
    "@docusaurus/core": "^3.10.0",
    "@docusaurus/faster": "^3.10.0",
    "@docusaurus/preset-classic": "^3.10.0",
    "@easyops-cn/docusaurus-search-local": "^0.49.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.10.0",
    "@docusaurus/types": "^3.10.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  },
  "overrides": {
    "serialize-javascript": "^7.0.3"
  }
}
