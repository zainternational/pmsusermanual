---
sidebar_position: 4
---

# User Groups

User groups define permission sets:

- **Add / edit** groups
- **Assign permissions** (e.g. view reservation, add charge, run night audit)
- **Assign groups to users** so users inherit those permissions

Access via **Configuration → General Configuration → User Groups** (or equivalent).
